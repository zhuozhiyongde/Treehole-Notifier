name: updater
on:
  [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - env:
        UID: ${{ secrets.UID }}
        PASSWORD: ${{ secrets.PASSWORD }}
    - run : python3 TreeholeNotifier.py ${UID} ${PASSWORD}
    timeout-minutes: 5
